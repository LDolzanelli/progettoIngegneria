USE destinazioni;

SET FOREIGN_KEY_CHECKS = 0;

DROP TABLE IF EXISTS
  databasechangelog,
  databasechangeloglock,
  volunteer_available_dates,
  volunteers_visit_types,
  blocked_dates,
  month_collection_state,
  config,
  visit_days,
  bookings,
  visits,
  visit_types,
  location_addresses,
  locations,
  users,
  area_of_interest;

SET FOREIGN_KEY_CHECKS = 1;